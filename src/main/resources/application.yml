server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: valdir.c.junior
    password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    generate-ddl: true